<?php 
/*
<?php

/*la recherch de mots 

PHP Data Types
$integerVar = 42;
$floatVar = 3.14;
$stringVar = "Hello, World!";
$boolVar = true;
$arrayVar = array(1, 2, 3);
class MyClass {
    // class definition
}

$objectVar = new MyClass();

$nullVar = null;

8_Resource: Represents a special type holding a reference to an external resource (like a database connection).
ex=>$variable6 = fopen("example.txt", "r");=>Outputs: resource
it's a way of managing references to external entities

9_Callable: Represents a callback function or a method that can be called using the call_user_func() or call_user_func_array() functions.
*/

/*NULL
if ($variable === null) { 
    the === important to check if its null 

Unsetting Variables:
You can also use the unset() function to unset a variable and make it null.
Copy code
$variable = "some value";
unset($variable);
// Now $variable is null


function exampleFunction(?string $param): ?string {
    return $param;
}
In this example, 
the ?string notation indicates that the
 parameter and the return type can be either 
 a string or null.

*/

/* unset function


In PHP, the unset() function is used to destroy variables, freeing up the memory they occupy. Once a variable has been unset, it can no longer be referenced or used. Here's the basic syntax:

php
Copy code
unset($variable);
Here's an example of using unset():

php
Copy code
$myVariable = "Hello, World!";
echo $myVariable;  // Outputs: Hello, World!

unset($myVariable);

// Now, $myVariable has been unset and cannot be used.
// If you try to use it, you might get a Notice: Undefined variable error.
// echo $myVariable;  // This line would result in an error

$myArray = array("one", "two", "three");
unset($myArray[1]);  // Removes the element with key 1 ("two")

// Now, $myArray is array("one", "three")



*/














?>